name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    name: Build (${{ matrix.arch }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: macos-15
            arch: arm64
          - os: macos-13
            arch: amd64

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Install PortAudio
        run: brew install portaudio

      - name: Build
        run: go build -ldflags="-s -w" -o whisprgo .

      - name: Package
        run: |
          tar czf whisprgo-darwin-${{ matrix.arch }}.tar.gz whisprgo
          shasum -a 256 whisprgo-darwin-${{ matrix.arch }}.tar.gz \
            | awk '{print $1}' > whisprgo-darwin-${{ matrix.arch }}.sha256

      - uses: actions/upload-artifact@v4
        with:
          name: dist-${{ matrix.arch }}
          path: |
            whisprgo-darwin-${{ matrix.arch }}.tar.gz
            whisprgo-darwin-${{ matrix.arch }}.sha256

  release:
    name: Publish
    needs: build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          pattern: dist-*
          merge-multiple: true
          path: dist

      - name: Compute variables
        run: |
          echo "VERSION=${GITHUB_REF_NAME#v}" >> "$GITHUB_ENV"
          echo "SHA256_ARM64=$(cat dist/whisprgo-darwin-arm64.sha256)" >> "$GITHUB_ENV"
          echo "SHA256_AMD64=$(cat dist/whisprgo-darwin-amd64.sha256)" >> "$GITHUB_ENV"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            dist/whisprgo-darwin-arm64.tar.gz
            dist/whisprgo-darwin-amd64.tar.gz

      - name: Update Homebrew tap
        env:
          TAP_TOKEN: ${{ secrets.TAP_TOKEN }}
        run: |
          git clone https://x-access-token:${TAP_TOKEN}@github.com/mecejus/homebrew-tap.git

          mkdir -p homebrew-tap/Formula
          sed \
            -e "s/VERSION_PLACEHOLDER/$VERSION/g" \
            -e "s/SHA256_ARM64_PLACEHOLDER/$SHA256_ARM64/g" \
            -e "s/SHA256_AMD64_PLACEHOLDER/$SHA256_AMD64/g" \
            homebrew/whisprgo.rb.template \
            > homebrew-tap/Formula/whisprgo.rb

          cd homebrew-tap
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Formula/whisprgo.rb
          git diff --cached --quiet || git commit -m "whisprgo $VERSION"
          git push
