@AGENTS.md

<!--
  The instructions live in AGENTS.md so every coding agent reads the same file.
  Claude Code does not read AGENTS.md on its own, so this file imports it.
  Skills live in .agents/skills/; .claude/skills is a symlink to that directory.
  Put Claude-Code-only instructions below this comment, nowhere else.
-->
